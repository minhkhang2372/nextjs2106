import Link from 'next/link';

const Home = () => (
  <div>
    <h1>Welcome to Next.js on Vercel!</h1>
    <p>Visit the API route <Link href="/api/shopeexu"><a>/api/shopeexu</a></Link> to see the API in action.</p>
  </div>
);

export default Home;
